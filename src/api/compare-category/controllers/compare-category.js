'use strict';

/**
 * compare-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::compare-category.compare-category');
